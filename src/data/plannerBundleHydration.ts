import type { RecipeDataset } from '../domain/types'
import { hydratePrimaryPlan, type PrimaryPlanHydration } from './primaryPlanHydration'
import { readCharacterProfileResult, readChecklistResult, readInventoryResult, type CharacterProfileState, type ChecklistState, type InventoryState } from './storage'
export type PlannerBundleHydration = { status: 'ready'; primaryPlan: PrimaryPlanHydration; checklist: ChecklistState; inventory: InventoryState; characterProfile: CharacterProfileState } | { status: 'recovery-required'; components: string[]; errors: string[] }
export function hydratePlannerBundle(dataset: RecipeDataset, storage: Pick<Storage, 'getItem'> = localStorage): PlannerBundleHydration { const checklist = readChecklistResult(storage); const inventory = readInventoryResult(storage); const characterProfile = readCharacterProfileResult(storage); const primaryPlan = hydratePrimaryPlan(dataset, storage); const components: string[] = []; const errors: string[] = []; if (checklist.status === 'invalid-storage') components.push('checklist'); if (inventory.status === 'invalid-storage') components.push('inventory'); if (characterProfile.status === 'invalid-storage') components.push('character-profile'); if (primaryPlan.status === 'recovery-required') { components.push('plan-session'); errors.push(...primaryPlan.errors) } if (components.length) { if (components.some((component) => component !== 'plan-session')) errors.unshift('저장된 지원 상태 일부를 읽을 수 없습니다. 자동으로 덮어쓰지 않습니다.'); return { status: 'recovery-required', components, errors } } return { status: 'ready', primaryPlan, checklist: checklist.value, inventory: inventory.value, characterProfile: characterProfile.value } }
